use strict;
use warnings;

use lib 't/fixtures/app_lib';
use ProtoOps ();

print ProtoOps::constant_one(41, 99), "\n";

=pod

=head1 NAME

t/fixtures/proto_app.pl - provide the proto_app fixture used by the PAX test suite.

=head1 DESCRIPTION

This file is part of the maintained PAX Perl surface and exists to provide the proto_app fixture used by the PAX test suite.

=cut

